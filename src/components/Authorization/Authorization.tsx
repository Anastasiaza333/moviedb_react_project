import css from './Authorization.module.css'
const Authorization = () => {
    return (
        <div className={css.Authorization}>
            <span>Sign</span>
        </div>
    );
};
export default Authorization;